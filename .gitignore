# ── Private documents (drafts, write-ups, presentations) ──────────────────────
*.docx
*.doc
*.pptx
*.ppt
*.xlsx
*.xls

# ── Internal docs (brand guide, site structure, progress tracking) ────────────
_private/

# ── Private projects (not for public release) ────────────────────────────────
# Ignore everything in blue-owl/, except the public companion folder for the post.
blue-owl/*
!blue-owl/regulating-private-credit/

# US Debt/ is the local working folder for The Unraveling Guarantee: raw
# Part N notebooks, build scripts, scratch outputs, the original .docx
# draft. Only the cleaned companion in "the-unraveling-guarantee/" ships.
US Debt/

# _archive/ holds shelved working folders kept for future use (never
# public). The GOOGL equity deep dive (Google Research/) was moved here
# on 2026-07-08; the pre-model research arc is done, model build unstarted.
_archive/

# ── Per-folder drafts (use _drafts/ or _draft prefix inside any research folder)
_drafts/
_draft*

# ── Python / Jupyter ──────────────────────────────────────────────────────────
__pycache__/
*.pyc
.ipynb_checkpoints/

# ── Chart outputs (regenerate from notebooks) ─────────────────────────────────
outputs/
*.png

# OS / editor configs are handled by the global excludes file
# (see git config --global core.excludesfile)
